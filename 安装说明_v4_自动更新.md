# 记得 V4 · 自动更新版

## 这次最重要的更新
V4 重点解决“每次升级都要删除桌面 App、清 Safari 缓存、重新添加主屏幕”的问题。

新增：
- 自动检测 version.json 中的新版本
- Service Worker 新版自动接管
- HTML 页面优先联网获取，避免长期卡在旧页面
- 新版静态资源自动更新缓存
- 旧缓存自动清理
- 发现新版本后自动刷新
- 页面右上角显示当前版本号 V4

## 从 V3 升级到 V4
这是最后一次建议按原方式上传完整文件：
1. 解压 V4 压缩包。
2. 上传全部文件到原来的 GitHub 仓库 `jide-`，覆盖同名文件。
3. 等 GitHub Pages 发布完成。
4. 打开桌面上的“记得”。

V4 的 Service Worker 会尝试自动接管并刷新页面。
如果第一次仍停留在 V3，只需关闭“记得”再打开一次，通常不需要删除主屏幕图标或清 Safari 网站数据。

## V4 以后怎么升级
以后只需：
1. 把新版文件上传到 GitHub；
2. 等 Pages 发布；
3. 正常打开“记得”。

程序会自动检查版本并更新。
